/** Full transparent brand lockup (icon + typography in one SVG) */
export const BRAND_LOGO_PATH = "/brand/logo.svg";

/** Icon-only mark for compact admin / sidebar slots */
export const BRAND_LOGO_MARK_PATH = "/brand/logo-mark.png";

/** Cropped lockup viewBox (~438×165) — used for Next Image intrinsic sizing */
export const BRAND_LOCKUP_ASPECT_RATIO = 438 / 165;

/** Navbar lockup height (px) — tuned to h-20 bar + primary CTA */
export const BRAND_NAV_LOGO_HEIGHT_PX = 70;
